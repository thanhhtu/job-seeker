## Setup project (venv + Playwright)

```bash
# 1. Create virtual environment
python3 -m venv .venv

# 2. Activate venv
source .venv/bin/activate      # Mac/Linux
.venv\Scripts\activate         # Windows

# 3. Install dependencies
pip install -r requirements.txt